package tictactoe.strategy;

import java.util.Collection;

import tictactoe.Board;
import tictactoe.Move;

/**
 * Find moves where two moves exist on the same row/col/diagonal as a move already played
 * @author andrea
 */
public class SetUpStrategy implements Strategy {
// TODO figure out how to determine where the player whose turn it is has already played
	@Override
	public Collection<Move> getMoves(Board board) {
		// TODO Auto-generated method stub
		return null;
	}

}
